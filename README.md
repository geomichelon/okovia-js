# JavaScript SDK

Client-side SDK for web applications.

Responsibilities:

- Create or propagate `operation_id`.
- Send client events with product context.
- Attach feature, channel, tenant, user, plan, and metadata.
- Avoid collecting prompt or response payloads by default.

This SDK supports the MVP dashboard by connecting user-facing product actions to backend usage events.
